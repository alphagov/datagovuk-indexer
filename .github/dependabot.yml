# Config for Dependabot updates. See Documentation here:
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  # Update GitHub actions in workflows
  - package-ecosystem: 'github-actions'
    directory: '/'
    # Every weekday
    schedule:
      interval: 'daily'
    # Ensure proposed updates are 2 weeks old to stay behind the absolute bleeding edge
    cooldown:
      default-days: 14
    groups:
      github-actions:
        patterns:
          - '*'

  - package-ecosystem: 'docker'
    # Look for a `Dockerfile` in the listed directories
    directories:
      - 'docker/'
    # Every weekday
    schedule:
      interval: 'daily'
    # Ensure proposed updates are 2 weeks old to stay behind the absolute bleeding edge
    cooldown:
      default-days: 14

  # Enable version updates for Docker Compose files
  - package-ecosystem: 'docker-compose'
    directories:
      - '/'
    # Every weekday
    schedule:
      interval: 'daily'
    # Ensure proposed updates are 2 weeks old to stay behind the absolute bleeding edge
    cooldown:
      default-days: 14

  # Enable version updates for Python/uv
  - package-ecosystem: 'uv'
    # Look for a `pyproject.toml` in the `root` directory
    directory: '/'
    # Every weekday
    schedule:
      interval: 'daily'
    # Ensure proposed updates are 2 weeks old to stay behind the absolute bleeding edge
    cooldown:
      default-days: 14
    groups:
      python:
        update-types:
          - 'minor'
          - 'patch'
